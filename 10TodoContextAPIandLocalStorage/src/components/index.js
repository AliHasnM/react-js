// Importing the TodoForm and TodoItem components from their respective files
import TodoForm from "./TodoForm";
import TodoItem from "./TodoItem";

// Exporting the imported components together from a single file
// Reason: This allows for easy import elsewhere in the app,
// enabling you to import both components at once.
export { TodoForm, TodoItem };
