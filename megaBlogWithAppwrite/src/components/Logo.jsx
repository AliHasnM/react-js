/* eslint-disable react/prop-types */
/* eslint-disable no-unused-vars */

// Logo component with a default width prop
const Logo = ({ width = "100px" }) => {
  // `width` is customizable via props, default is "100px" for consistent styling.
  return <div>Logo</div>; // Placeholder for the actual logo, can be replaced with an image or styled component.
};

export default Logo;
